import { StateTypeToStringPipe } from './state-type-to-string.pipe';

describe('StateTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new StateTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
